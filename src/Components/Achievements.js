import React from "react";

const Achievements = ({ achievements }) => {
  return (
    <section className="achievements">
      <h2 className="section-heading">I have achieved</h2>
      {/* use vertical timeline component here*/}
    </section>
  );
};

export default Achievements;
